import React from 'react'
import { montserrat } from '../ui/fonts'
const page = () => {
  return (
    <main className="flex flex-col p-6">
        <h1 className={`${montserrat.className} font-extrabold text-3xl`}>Dashboard</h1>
    </main>    
  )
}

export default page
